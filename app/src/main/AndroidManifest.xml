<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yj.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".selection" />
        <activity android:name=".gp" />
        <activity android:name=".avanue" />
        <activity android:name=".gpg" />
        <activity android:name=".pp" />
        <activity android:name=".ts" />
        <activity android:name=".sq" />
        <activity android:name=".qb" />
        <activity android:name=".pm"></activity>
    </application>

</manifest>